{
  "name": "k-urtica.github.io",
  "version": "1.0.0",
  "description": "K portfolio site.",
  "author": "K",
  "private": true,
  "scripts": {
    "dev": "nuxt dev",
    "build": "nuxt build",
    "preview": "nuxt preview",
    "generate": "nuxt generate",
    "analyze": "nuxt analyze",
    "lint": "eslint src/ --ext .ts,.js,.vue --max-warnings=0 --ignore-path .gitignore",
    "lint-fix": "yarn prettier-fix && eslint src/ --fix --ext .ts,.js,.vue --max-warnings=0 --ignore-path .gitignore",
    "prettier-check": "prettier --check src/",
    "prettier-fix": "prettier --write src/"
  },
  "dependencies": {
    "twemoji": "^14.0.2"
  },
  "devDependencies": {
    "@mdi/js": "^7.4.47",
    "@nuxt/eslint-config": "^0.2.0",
    "@nuxtjs/tailwindcss": "^6.11.4",
    "eslint": "^8.56.0",
    "eslint-config-prettier": "^9.1.0",
    "nuxt": "^3.10.2",
    "nuxt-simple-sitemap": "^4.1.7",
    "prettier": "^3.2.5",
    "prettier-plugin-tailwindcss": "^0.5.11"
  }
}
