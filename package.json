{
  "name": "k-urtica.github.io",
  "version": "1.0.0",
  "description": "K portfolio site.",
  "author": "K",
  "private": true,
  "scripts": {
    "dev": "nuxt dev",
    "build": "nuxt build",
    "preview": "nuxt preview",
    "generate": "nuxt generate",
    "analyze": "nuxt analyze",
    "lint": "eslint . --ext .ts,.js,.vue --max-warnings=0 --ignore-path .gitignore",
    "lint-fix": "yarn prettier-fix && eslint . --fix --ext .ts,.js,.vue --max-warnings=0 --ignore-path .gitignore",
    "prettier-check": "prettier --check .",
    "prettier-fix": "prettier --write ."
  },
  "dependencies": {
    "twemoji": "^14.0.2"
  },
  "devDependencies": {
    "@mdi/js": "^7.2.96",
    "@nuxt/eslint-config": "^0.1.1",
    "@nuxt/image": "^1.0.0-rc.1",
    "@nuxtjs/tailwindcss": "^6.8.0",
    "eslint": "^8.47.0",
    "eslint-config-prettier": "^9.0.0",
    "nuxt": "^3.6.5",
    "prettier": "^3.0.2",
    "prettier-plugin-tailwindcss": "^0.5.3"
  }
}
